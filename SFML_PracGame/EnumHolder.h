#pragma once

namespace Enums
{
	enum class EnemyTypes
	{
		light,
		Heavy
	};

	enum class FacingDirection
	{
		Right,
		Left
	};

	enum class AnimationStates
	{
		Idle,
		Run,
		Attack,
		Death
	};
}

